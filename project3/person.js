// export const name = "Jesse";
// export const age = 40;

// anather way to do this

// export{name,age}

const message = () => {
    const name = "Jesse";
    const age = 40;
    return name + ' is ' + age + 'years old.';
    };
    
export default message;